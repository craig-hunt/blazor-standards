{
  "//": [
    "The shape of this project's secrets. Never fill this file in and never",
    "commit a filled copy. It exists so the same key names reach every target.",
    "",
    "LOCAL DEVELOPMENT, from src/App:",
    "  dotnet user-secrets init",
    "  dotnet user-secrets set \"App:AdminPassword\" \"value\"",
    "  dotnet user-secrets list",
    "Values land in the user profile, outside the repository entirely.",
    "",
    "THE REGRESSION SUITE reads its own keys, from tests/Regression.Tests:",
    "  dotnet user-secrets set \"App:BaseUrl\" \"https://staging.example.com\"",
    "",
    "DEPLOYED AND CADENCED RUNS inject the same keys as environment variables.",
    ".NET maps a colon to a double underscore, so App:AdminPassword arrives as",
    "App__AdminPassword. See the mapping table in the README."
  ],

  "App": {
    "BaseUrl": "http://localhost:5280",
    "AdminUser": "",
    "AdminPassword": "",
    "ApiToken": ""
  }
}
